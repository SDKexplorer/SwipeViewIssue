<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="SwipeViewIssue.MainPage">

    <StackLayout Padding="10">
        <!-- Place new controls here -->
        <SwipeView>
            <SwipeView.LeftItems>
                <SwipeItems>
                    <SwipeItem Text="X" BackgroundColor="DarkBlue"/>
                </SwipeItems>
            </SwipeView.LeftItems>
            <SwipeView.RightItems>
                <SwipeItems>
                    <SwipeItem Text="Y" BackgroundColor="Plum"/>
                </SwipeItems>
            </SwipeView.RightItems>
            <StackLayout Padding="10" BackgroundColor="Crimson">
                <Label Text=" Swipe works, tapping on content doesn't "/>
                <Entry Text="TAP ME"></Entry>
                <Button Text="TAP ME"></Button>
                <CheckBox></CheckBox>
                <DatePicker></DatePicker>
            </StackLayout>
        </SwipeView>
    </StackLayout>

</ContentPage>
